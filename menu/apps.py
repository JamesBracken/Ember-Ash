from django.apps import AppConfig


class MenuConfig(AppConfig):
    """
    Creates the configuration for the menu app
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'menu'
    